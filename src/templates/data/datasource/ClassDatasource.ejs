import { BaseHttpDatasource, HttpService } from '@azure-net/kit';

export class <%= name %>Datasource extends BaseHttpDatasource {
	constructor() {
		super({ http: new HttpService({ baseUrl: '' }) });
	}
}