import { browser } from '$app/environment';
export default {
    current() {
        if (!browser) {
            throw new Error('AsyncLocalStorage has not been initialized');
        }
        return { data: {} };
    }
};
